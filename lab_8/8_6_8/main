#include <iostream>
#include "CTree.h"

using namespace std;

int main()
{
    int a[9] = {1, 3, 0, 7, 10, 2, 5, 4, 8};

    CTree <int> first(a, 9);
    int n = 0, k = 0, l = 0;
    cout<< endl<<endl;

    first.print_tree_level(first.top_, 0, n);
    cout<<n<<endl;

    return 0;
}
